---
// Footer: closing HUD block on the page ground. Company data, section
// anchors, product links, privacy + socials (icons only, no card grid).
import Icon from "@/components/icon.astro";
import { getCompanyHome, privacyPath } from "@/data/company-home";
import { getLocalePath, type Locale } from "@/i18n/utils";

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const i = getCompanyHome(locale);
const f = i.footer;
const homePath = getLocalePath(locale);
const privacyHref = privacyPath(locale);
---

<footer class="border-t border-primary/20 text-white/65">
  <div class="mx-auto max-w-6xl px-6 pb-8 pt-10">
    <div class="grid gap-10 md:grid-cols-[1.4fr_1fr_1fr_1fr]">
      <div>
        <a href={homePath} class="inline-flex items-center gap-2.5">
          <img src="/logo.png" alt="" width="28" height="28" class="h-7 w-7" loading="lazy" />
          <span class="font-mono text-sm font-semibold tracking-tight text-white">AwesomeWorks</span>
        </a>
        <p class="mt-4 max-w-[36ch] text-sm leading-relaxed">{f.brandLine}</p>
        <p class="mt-4 text-xs leading-relaxed text-white/45">{f.legalLine}</p>
      </div>

      <nav aria-label={f.companyTitle}>
        <p class="font-mono text-[11px] font-semibold uppercase tracking-[0.2em] text-white/45">{f.companyTitle}</p>
        <ul class="mt-4 space-y-2.5">
          {
            f.companyLinks.map((link) => (
              <li>
                <a href={`${homePath}${link.href}`} class="text-sm transition hover:text-white">
                  {link.label}
                </a>
              </li>
            ))
          }
        </ul>
      </nav>

      <nav aria-label={f.productsTitle}>
        <p class="font-mono text-[11px] font-semibold uppercase tracking-[0.2em] text-white/45">{f.productsTitle}</p>
        <ul class="mt-4 space-y-2.5">
          {
            f.productLinks.map((link) => (
              <li>
                <a href={link.href} target="_blank" rel="noopener" class="text-sm transition hover:text-white">
                  {link.label}
                </a>
              </li>
            ))
          }
        </ul>
      </nav>

      <div>
        <p class="font-mono text-[11px] font-semibold uppercase tracking-[0.2em] text-white/45">{f.legalTitle}</p>
        <ul class="mt-4 space-y-2.5">
          <li>
            <a href={privacyHref} class="text-sm transition hover:text-white">{f.privacyLabel}</a>
          </li>
        </ul>
        <ul class="mt-5 flex items-center gap-3">
          {
            f.socials.map((social) => (
              <li>
                <a
                  href={social.href}
                  target="_blank"
                  rel="noopener"
                  aria-label={social.name}
                  class="grid h-9 w-9 place-items-center border border-primary/35 text-white/65 transition hover:border-primary hover:text-white"
                >
                  <Icon name={social.icon} class="h-4 w-4" />
                </a>
              </li>
            ))
          }
        </ul>
      </div>
    </div>

    <p class="mt-10 border-t border-primary/20 pt-6 text-xs text-white/45">{f.copyright}</p>
  </div>
</footer>
